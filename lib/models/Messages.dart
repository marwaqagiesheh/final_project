import 'package:flutter/cupertino.dart';

class Messages {

  int ? ID;
  String ? Body;
  DateTime ? Date_Time;
  int ?Recever_ID;
  int ?Sender_ID;

  Messages(this.ID, this.Body, this.Date_Time, this.Recever_ID, this.Sender_ID);
}
