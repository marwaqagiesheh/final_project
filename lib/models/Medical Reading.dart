import 'package:flutter/cupertino.dart';

class Medical_Reading {

  int ? Reading_ID;
  double ? Result;
  DateTime ? Date_time;
  String ? Reading_type;

  Medical_Reading({this.Reading_ID, this.Result, this.Date_time,this.Reading_type});

}